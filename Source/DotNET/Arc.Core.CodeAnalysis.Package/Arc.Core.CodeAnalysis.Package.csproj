<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <!-- Package-only project: it carries no source of its own, only the standalone
             'Cratis.Arc.Core.CodeAnalysis' package identity that bundles the analyzer assembly
             (Arc.Core.CodeAnalysis.csproj) and its code fixes (Arc.Core.CodeAnalysis.CodeFixes.csproj) together,
             exactly as they shipped from one assembly before the two were split apart to keep RS1038 (Workspaces
             references in a compiler-loaded assembly) out of the analyzer. Consumers of this standalone package -
             as opposed to the 'Cratis.CodeAnalysis' umbrella - see no change from the split. -->
        <PackageId>Cratis.Arc.Core.CodeAnalysis</PackageId>
        <AssemblyName>Cratis.Arc.Core.CodeAnalysis.Package</AssemblyName>
        <IncludeBuildOutput>false</IncludeBuildOutput>
        <IncludeSymbols>false</IncludeSymbols>
        <IncludeSource>false</IncludeSource>
        <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
        <DevelopmentDependency>true</DevelopmentDependency>
        <TargetFramework>netstandard2.0</TargetFramework>
        <TargetFrameworks></TargetFrameworks>
        <NoWarn>$(NoWarn);NU5128</NoWarn>
    </PropertyGroup>

    <!-- Build-order-only references: neither assembly is linked into this package-only project's own (empty)
         compilation, they are only required to exist in their bin output before Pack runs. -->
    <ItemGroup>
        <ProjectReference Include="../Arc.Core.CodeAnalysis/Arc.Core.CodeAnalysis.csproj"
                          OutputItemType="Analyzer" ReferenceOutputAssembly="false" PrivateAssets="all" />
        <ProjectReference Include="../Arc.Core.CodeAnalysis.CodeFixes/Arc.Core.CodeAnalysis.CodeFixes.csproj"
                          ReferenceOutputAssembly="false" PrivateAssets="all" />
    </ItemGroup>

    <ItemGroup>
        <None Include="../Arc.Core.CodeAnalysis/bin/$(Configuration)/netstandard2.0/Cratis.Arc.Core.CodeAnalysis.dll"
              Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
        <None Include="../Arc.Core.CodeAnalysis.CodeFixes/bin/$(Configuration)/netstandard2.0/Cratis.Arc.Core.CodeAnalysis.CodeFixes.dll"
              Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
    </ItemGroup>
</Project>
